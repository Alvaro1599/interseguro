import { QRFactorization } from '@interseguro-test/utils';
import { Matrix } from '@interseguro-test/models';

export class MatrixProcessService {
  public process(matrix: Partial<Matrix>): Matrix {
    return matrix;
  }
}
